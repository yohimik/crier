# Generated by tools/gendocs. Do not edit; edit internal/config/registry.go.
#
# Every crier option, with its default. Copy the lines you want into your own
# config — this file is a reference rather than a starting point: it turns
# nothing on, and the secrets are placeholders.
#
# The same keys are settable as environment variables (CRIER_ + the key, dots
# and dashes to underscores) and as flags (the key with dots turned into
# dashes). See docs/configuration/README.md.

http:
  # initial backoff delay between retries
  retry-base-delay: "500ms"
  # maximum number of retries for retryable requests
  retry-max: 3
  # maximum backoff delay between retries
  retry-max-delay: "10s"
  # per-request HTTP timeout for calls that carry no media
  timeout: "60s"
  # per-request HTTP timeout for a request carrying media; covers the whole upload
  upload-timeout: "10m"
log:
  # log format: console or json
  format: "console"
  # log level: trace, debug, info, warn, error
  level: "info"
publish:
  boosty:
    # who can read the post: free, paid (a one-time price) or level (a subscription tier)
    access: "free"
    # Boosty access token, taken from a signed-in browser session; it expires
    access-token: "<your-access-token>"
    # Boosty API base URL; the API is unofficial and undocumented, so it is configurable
    api-base-url: "https://api.boosty.to"
    # blog to post in: the slug in boosty.to/<blog>
    blog: ""
    # Boosty specific post text
    caption: ""
    # currency publish.boosty.price is quoted in, sent as the X-Currency header
    currency: "RUB"
    # the browser session's device id, which a refresh will not run without
    device-id: "<your-device-id>"
    # publish to a Boosty blog
    enabled: false
    # how the render is made to match boosty's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for boosty, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for boosty; 0 inherits render.height
    height: 0
    # not available: a boosty post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # subscription level id the post is limited to; required when access is level
    level-id: ""
    # post at most this many pages to boosty at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: boosty has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for boosty only, after the global ones
    overlay: []
    # one-time price the post is unlocked by, in publish.boosty.currency; required when access is paid
    price: 0
    # Boosty refresh token; with device-id it renews an expired access token, and boosty replaces it every time it is used
    refresh-token: "<your-refresh-token>"
    # Boosty post title; empty uses the caption's first line
    title: ""
    # Boosty upload host; media goes here rather than to the API host
    upload-base-url: "https://upload.boosty.to"
    # render width for boosty; 0 inherits render.width
    width: 0
  # caption used by every platform that has no caption of its own
  caption: ""
  # how many platforms are published to at the same time
  concurrency: 4
  discord:
    # Discord specific message content
    caption: ""
    # publish to a Discord webhook
    enabled: false
    # how the render is made to match discord's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for discord, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for discord; 0 inherits render.height
    height: 0
    # not available: a discord post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to discord at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # allow @everyone in the Discord message to notify the channel
    mention-everyone: false
    # audio file attached to the discord post, overriding publish.music-file for this platform alone
    music-file: ""
    # template overlays applied for discord only, after the global ones
    overlay: []
    # override the webhook's display name
    username: ""
    # full Discord webhook URL (it is the credential)
    webhook-url: "<your-webhook-url>"
    # render width for discord; 0 inherits render.width
    width: 0
  # render and validate only; make no network calls
  dry-run: false
  facebook:
    # Facebook Graph API base URL
    api-base-url: "https://graph.facebook.com/v25.0"
    # Facebook specific caption
    caption: ""
    # publish to a Facebook Page
    enabled: false
    # how the render is made to match facebook's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for facebook, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for facebook; 0 inherits render.height
    height: 0
    # not available: a facebook post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to facebook at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: facebook has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for facebook only, after the global ones
    overlay: []
    # Facebook Page id
    page-id: ""
    # publish as a Page story instead of a photo post
    story: false
    # Facebook Page access token
    token: "<your-token>"
    # send the staged URL instead of uploading the bytes
    use-url: false
    # render width for facebook; 0 inherits render.width
    width: 0
  # publish this existing file instead of rendering one
  input: ""
  instagram:
    # Instagram Graph API base URL
    api-base-url: "https://graph.facebook.com/v25.0"
    # Instagram specific caption
    caption: ""
    # add a sixteen-second cover story with render.video audio alongside the feed post
    cover-story: false
    # publish to Instagram
    enabled: false
    # how the render is made to match instagram's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for instagram, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for instagram; 0 inherits render.height
    height: 0
    # video file posted as the first item of the instagram post, ahead of the pages; it counts towards the attachment limit
    lead-video: ""
    # post at most this many pages to instagram at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: instagram has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for instagram only, after the global ones
    overlay: []
    # how often the media container status is polled
    poll-interval: "2s"
    # how long to wait for the media container to be ready
    poll-timeout: "2m"
    # publish as a story instead of a feed post
    story: false
    # Instagram access token
    token: "<your-token>"
    # Instagram professional account user id
    user-id: ""
    # render width for instagram; 0 inherits render.width
    width: 0
  linkedin:
    # LinkedIn REST API base URL
    api-base-url: "https://api.linkedin.com"
    # author URN, e.g. urn:li:person:XXXX or urn:li:organization:NNNN
    author-urn: ""
    # LinkedIn specific commentary
    caption: ""
    # publish to LinkedIn
    enabled: false
    # how the render is made to match linkedin's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for linkedin, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for linkedin; 0 inherits render.height
    height: 0
    # not available: a linkedin post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to linkedin at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: linkedin has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for linkedin only, after the global ones
    overlay: []
    # LinkedIn OAuth 2.0 access token
    token: "<your-token>"
    # value of the mandatory LinkedIn-Version header
    version: "202606"
    # render width for linkedin; 0 inherits render.width
    width: 0
  mastodon:
    # media description used for accessibility
    alt-text: ""
    # Mastodon instance base URL, e.g. https://mastodon.social
    api-base-url: ""
    # Mastodon specific status text
    caption: ""
    # publish to Mastodon
    enabled: false
    # how the render is made to match mastodon's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for mastodon, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for mastodon; 0 inherits render.height
    height: 0
    # not available: a mastodon post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to mastodon at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: mastodon has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for mastodon only, after the global ones
    overlay: []
    # how often a still-processing attachment is polled
    poll-interval: "2s"
    # how long to wait for the attachment to finish processing
    poll-timeout: "2m"
    # Mastodon access token
    token: "<your-token>"
    # status visibility: public, unlisted, private or direct
    visibility: "public"
    # render width for mastodon; 0 inherits render.width
    width: 0
  # audio file attached to the post on the platforms whose API can carry one (discord, slack, telegram); empty attaches none
  music-file: ""
  reddit:
    # Reddit API base URL (the OAuth host)
    api-base-url: "https://oauth.reddit.com"
    # Reddit token endpoint base URL
    auth-base-url: "https://www.reddit.com"
    # Reddit specific text; used as the title when no title is set
    caption: ""
    # Reddit script app client id
    client-id: "<your-client-id>"
    # Reddit script app client secret
    client-secret: "<your-client-secret>"
    # publish to Reddit
    enabled: false
    # how the render is made to match reddit's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for reddit, and what transparency is flattened onto
    fit-background: "#ffffff"
    # flair template id applied to the post
    flair-id: ""
    # render height for reddit; 0 inherits render.height
    height: 0
    # post kind: auto, image, video or link
    kind: "auto"
    # not available: a reddit post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to reddit at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: reddit has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # mark the post NSFW
    nsfw: false
    # template overlays applied for reddit only, after the global ones
    overlay: []
    # Reddit account password, for the script-app password grant
    password: "<your-password>"
    # how often the new post is looked for after submitting
    poll-interval: "2s"
    # how long to look for the new post's permalink
    poll-timeout: "30s"
    # Reddit refresh token; when set it is used instead of the password grant
    refresh-token: "<your-refresh-token>"
    # mark the post a spoiler
    spoiler: false
    # subreddit to post to, without the r/ prefix
    subreddit: ""
    # Reddit post title
    title: ""
    # override the User-Agent; empty builds the descriptive one Reddit requires
    user-agent: ""
    # Reddit account name, also used in the mandatory User-Agent
    username: ""
    # render width for reddit; 0 inherits render.width
    width: 0
  slack:
    # Slack Web API base URL
    api-base-url: "https://slack.com/api"
    # Slack specific caption
    caption: ""
    # Slack channel ID to post in, such as C0123ABCD; the bot has to be in it
    channel: ""
    # publish to Slack
    enabled: false
    # how the render is made to match slack's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for slack, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for slack; 0 inherits render.height
    height: 0
    # not available: a slack post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to slack at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # audio file attached to the slack post, overriding publish.music-file for this platform alone
    music-file: ""
    # template overlays applied for slack only, after the global ones
    overlay: []
    # Slack bot token (xoxb-…) with files:write and chat:write
    token: "<your-token>"
    # render width for slack; 0 inherits render.width
    width: 0
  telegram:
    # Telegram Bot API base URL
    api-base-url: "https://api.telegram.org"
    # Telegram specific caption
    caption: ""
    # Telegram chat id or @channelusername
    chat-id: ""
    # publish to Telegram
    enabled: false
    # how the render is made to match telegram's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for telegram, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for telegram; 0 inherits render.height
    height: 0
    # video file posted as the first item of the telegram post, ahead of the pages; it counts towards the attachment limit
    lead-video: ""
    # post at most this many pages to telegram at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # audio file attached to the telegram post, overriding publish.music-file for this platform alone
    music-file: ""
    # template overlays applied for telegram only, after the global ones
    overlay: []
    # Telegram bot token
    token: "<your-token>"
    # render width for telegram; 0 inherits render.width
    width: 0
  threads:
    # Threads API base URL
    api-base-url: "https://graph.threads.net/v1.0"
    # Threads specific post text
    caption: ""
    # publish to Threads
    enabled: false
    # how the render is made to match threads's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for threads, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for threads; 0 inherits render.height
    height: 0
    # not available: a threads post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to threads at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: threads has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for threads only, after the global ones
    overlay: []
    # how often the media container status is polled
    poll-interval: "2s"
    # how long to wait for the media container to be ready
    poll-timeout: "2m"
    # Threads access token, with the threads_basic and threads_content_publish scopes; an Instagram token is not one
    token: "<your-token>"
    # Threads account user id, as /me reports it
    user-id: ""
    # render width for threads; 0 inherits render.width
    width: 0
  tiktok:
    # TikTok Content Posting API base URL
    api-base-url: "https://open.tiktokapis.com"
    # let TikTok put a recommended track under a photo post; no API anywhere names a specific licensed track
    auto-add-music: false
    # TikTok post description
    caption: ""
    # publish to TikTok
    enabled: false
    # how the render is made to match tiktok's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for tiktok, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for tiktok; 0 inherits render.height
    height: 0
    # not available: a tiktok post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to tiktok at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: tiktok has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for tiktok only, after the global ones
    overlay: []
    # how often the publish status is polled
    poll-interval: "2s"
    # how long to wait for TikTok to finish the upload
    poll-timeout: "2m"
    # TikTok privacy level: SELF_ONLY, MUTUAL_FOLLOW_FRIENDS, FOLLOWER_OF_CREATOR or PUBLIC_TO_EVERYONE
    privacy-level: "SELF_ONLY"
    # TikTok post title
    title: ""
    # TikTok access token
    token: "<your-token>"
    # render width for tiktok; 0 inherits render.width
    width: 0
  vk:
    # VK API base URL; methods are called under /method/
    api-base-url: "https://api.vk.com"
    # value of the mandatory VK API version parameter
    api-version: "5.199"
    # VK specific post text
    caption: ""
    # publish to VK
    enabled: false
    # how the render is made to match vk's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for vk, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for vk; 0 inherits render.height
    height: 0
    # not available: a vk post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to vk at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: vk has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for vk only, after the global ones
    overlay: []
    # wall the post lands on: negative for a community, such as -123456, positive for a user
    owner-id: 0
    # VK access token with the wall, photos, video and docs scopes
    token: "<your-token>"
    # render width for vk; 0 inherits render.width
    width: 0
  x:
    # X API base URL
    api-base-url: "https://api.x.com"
    # X specific post text
    caption: ""
    # publish to X
    enabled: false
    # how the render is made to match x's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for x, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for x; 0 inherits render.height
    height: 0
    # not available: a x post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to x at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: x has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for x only, after the global ones
    overlay: []
    # how often a not-ready tweet create is retried
    poll-interval: "2s"
    # how long to keep retrying a tweet whose media is not ready
    poll-timeout: "30s"
    # X OAuth 2.0 user access token
    token: "<your-token>"
    # render width for x; 0 inherits render.width
    width: 0
  youtube:
    # YouTube Data API v3 base URL
    api-base-url: "https://www.googleapis.com"
    # Google OAuth 2.0 token endpoint base URL
    auth-base-url: "https://oauth2.googleapis.com"
    # YouTube video description
    caption: ""
    # YouTube category id; 22 is "People & Blogs"
    category-id: "22"
    # Google Cloud OAuth client id
    client-id: "<your-client-id>"
    # Google Cloud OAuth client secret
    client-secret: "<your-client-secret>"
    # publish to YouTube (videos only)
    enabled: false
    # how the render is made to match youtube's frame: none, cover, contain or stretch; anything but none needs width and height
    fit: "none"
    # hex colour behind a contain letterbox for youtube, and what transparency is flattened onto
    fit-background: "#ffffff"
    # render height for youtube; 0 inherits render.height
    height: 0
    # not available: a youtube post is pictures or a video and never both, so a value here is refused rather than ignored
    lead-video: ""
    # post at most this many pages to youtube at once; 0 uses the platform's own limit, which is also the ceiling
    max-attachments: 0
    # not available: youtube has no API for attaching an audio file, so a value here is refused rather than ignored
    music-file: ""
    # template overlays applied for youtube only, after the global ones
    overlay: []
    # video privacy: private, unlisted or public; an API project Google has not audited gets private whatever is asked for
    privacy-status: "private"
    # OAuth refresh token carrying the youtube.upload scope; it is traded for an access token each run
    refresh-token: "<your-refresh-token>"
    # JPEG or PNG set as the video's thumbnail; it needs a phone-verified channel, so a refusal is a warning rather than a failed post
    thumbnail: ""
    # YouTube video title; empty uses the caption's first line, cut to 100 characters
    title: ""
    # render width for youtube; 0 inherits render.width
    width: 0
render:
  # background colour transparent pixels are flattened onto for JPEG
  background: "#ffffff"
  # base URL, or a directory, that relative resources in the template resolve against
  base-url: ""
  # extra stylesheet files applied after the document's own CSS
  css: []
  # where the template's data comes from: a JSON or YAML file, "-" for stdin, or "env:PREFIX" to build it from the environment
  data: ""
  # extra directories scanned for fonts
  fonts-dir: []
  # output image format: png or jpeg
  format: "png"
  # output height in CSS pixels (0 lets the document's @page rule decide)
  height: 1920
  # ignore system fonts and use only the embedded Go fonts (deterministic output)
  hermetic-fonts: false
  # JPEG quality, 1 to 100
  jpeg-quality: 90
  # CSS media type used for the cascade: screen or print
  media-type: "screen"
  # output file path; empty writes into a temporary directory
  output: ""
  # template files parsed after the base one, redefining its {{block}} sections
  overlays: []
  # most pages a document may lay out into; content past it is refused (hard cap 20)
  pages-max: 10
  # a pool of base templates; one is chosen at random per run
  pool: []
  # device pixel ratio: output pixels per CSS pixel (max 4)
  scale: "1"
  # seed for the template randomisation; 0 draws a new one and logs it
  seed: 0
  # extra supersampling factor applied on top of scale, then downsampled
  supersample: 1
  # path to the Go html/template file to render
  template: ""
  video:
    # audio file mixed into the video
    audio: ""
    # repeat the audio for as long as the video runs
    audio-loop: false
    # a pool of audio files; one is chosen at random per run
    audio-pool: []
    # output codec preset: h264, h265, vp9 or none to rely on ffmpeg-args alone
    codec-preset: "h264"
    # video length; ignored when render.video.frames is set
    duration: "3s"
    # render an animated template into a clip instead of a still image
    enabled: false
    # extra ffmpeg arguments, inserted before the output file
    ffmpeg-args: []
    # ffmpeg executable, resolved on PATH; ffmpeg is a prerequisite crier does not bundle
    ffmpeg-bin: "ffmpeg"
    # clip format: mp4 or gif
    format: "mp4"
    # video frame rate
    fps: 30
    # exact number of frames to render; 0 derives it from the duration and the frame rate
    frames: 0
    # directory or glob of existing frames to encode, instead of rendering them
    frames-input: ""
  # output width in CSS pixels (0 lets the document's @page rule decide)
  width: 1080
stage:
  # how to publish a public image URL: none, s3, server or url
  mode: "none"
  s3:
    # S3 access key id
    access-key: "<your-access-key>"
    # canned ACL applied to the object, e.g. "public-read"
    acl: ""
    # S3 bucket the image is uploaded to
    bucket: ""
    # delete the staged object once publishing is done
    delete-after: true
    # S3-compatible endpoint host, e.g. s3.amazonaws.com or localhost:9000
    endpoint: ""
    # key prefix inside the bucket
    prefix: ""
    # hand out a presigned URL instead of a public one
    presign: true
    # lifetime of the presigned URL
    presign-expiry: "1h"
    # base URL objects are publicly reachable at, when presign is false
    public-base-url: ""
    # S3 region
    region: "us-east-1"
    # S3 secret access key
    secret-key: "<your-secret-key>"
    # talk to the S3 endpoint over HTTPS
    use-ssl: true
  server:
    # address the built-in stage server listens on
    listen: "127.0.0.1:0"
    # base URL the stage server is reachable at from the internet
    public-url: ""
    # how long to wait for the stage server to shut down
    shutdown-timeout: "10s"
    tunnel:
      # ngrok local agent API polled for the public URL
      api-url: "http://127.0.0.1:4040/api/tunnels"
      # extra tunnel arguments; {port} and {addr} are substituted
      args: []
      # tunnel executable; defaults to the mode's own name, resolved on PATH
      bin: ""
      # expose the stage server publicly: none, ngrok, zrok or custom
      mode: "none"
      # how long to wait for the tunnel to report a public URL
      startup-timeout: "30s"
      # regexp with one capture group finding the public URL in the tunnel output (custom mode)
      url-pattern: ""
  # pre-hosted image URL used when stage.mode is url
  url: ""
